/// Generates sealed class helper utils.
///
/// Sealed class can have limited number of subclasses. It's
/// basically an implementation of [Coproduct*] type.
class Sealed {
  const Sealed();
}

const sealed = Sealed();
